# encoding: utf-8
"""
Installing:
    pip install .
    pip install [-e] .

Building (and installing) for distribution:
    pip install build
    python -m build
    pip install dist/*.whl

Testing:
    python -c "import fabmanager; print('ok')"

Publish to PyPI:
    pip install twine
    twine upload dist/*
"""
